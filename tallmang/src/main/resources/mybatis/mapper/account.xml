<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tallmang.mybatis.mapper.AccountMapper">
	<select id="getUser" resultType="com.tallmang.mybatis.vo.AccountVO">
		select * from Account
	</select>
</mapper>